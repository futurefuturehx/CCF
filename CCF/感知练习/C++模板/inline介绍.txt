1.引入inline关键字的原因：
在c/c++中 为了解决一些频繁调用的小函数大量消耗空间的问题
特别引入了inline修饰符 表示内联函数
栈空间是有限的 加入频繁大量使用 就会造成空间不足而导致出错
其实 在内部的作用就是在每个for循环的内部任何调用dbtest(i)的地方都换成了(i%2>0)?奇：偶
这样就避免了频繁调用函数对栈内存重复开辟所带来的消耗

也就是说 
inline int max(int a,int b)
{
    return a>b?a:b;
}
调用:cout<<max(a,b)<<endl;
在编译时展开为:cout<<(a>b?a:b)<<endl;